---
import Layout from "../layouts/Layout.astro";
---

<Layout
  description={`Søk etter oppskrifter`}
  tab_title={`Søk etter oppskrifter`}
  seo_title="Søk etter oppskrifter"
  seo_image=`${Astro.url.origin}/og/search.png`
>
  <link href="/pagefind/pagefind-ui.css" rel="stylesheet" />
  <script src="/pagefind/pagefind-ui.js" is:inline></script>

  <div
    class="mt-4 max-w-sm rounded-lg border-2 border-solid border-black px-8 py-4 pb-2 pt-4"
  >
    <h4 class="text-lg font-bold">Søk etter oppskrift</h4>
    <div class="py-2" id="search"></div>
  </div>
</Layout>
<script>
  window.addEventListener("DOMContentLoaded", (event) => {
    // @ts-ignore
    new PagefindUI({
      element: "#search",
      showSubResults: false,
      showImages: false,
      debounceTimeoutMs: 300,
      filteringOptions: {
        perfectMatchesOnly: false,
        splitWords: true,
        minimumWordLength: 2,
      },
    });
  });
</script>

<style is:global>
  nav {
    display: grid;
    place-items: center;
  }

  .pagefind-ui {
    --pagefind-ui-scale: 0.9;
    --pagefind-ui-primary: navy;
    --pagefind-ui-text: black;
    --pagefind-ui-border: slategrey;
    --pagefind-ui-border-width: 1px;
    --pagefind-ui-border-radius: 0.25rem;
    --pagefind-ui-font: Atkinson-Hyperlegible;

    max-width: 100%;
  }

  .pagefind-ui .pagefind-ui__drawer:not(.pagefind-ui__hidden) {
    position: absolute;
    left: 0;
    right: 0;
    margin-top: 0px;
    z-index: 9999;
    padding: 0 2em 1em;
    overflow-y: auto;
    box-shadow:
      0 10px 10px -5px rgba(0, 0, 0, 0.2),
      0 2px 2px 0 rgba(0, 0, 0, 0.1);
    border-bottom-right-radius: var(--pagefind-ui-border-radius);
    border-bottom-left-radius: var(--pagefind-ui-border-radius);
    background-color: var(--pagefind-ui-background);
  }

  .pagefind-ui .pagefind-ui__result-link {
    color: var(--pagefind-ui-primary);
  }

  .pagefind-ui .pagefind-ui__result-excerpt {
    color: var(--pagefind-ui-text);
  }
</style>
